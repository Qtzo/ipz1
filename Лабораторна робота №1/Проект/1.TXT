@startuml
left to right direction

actor користувач
actor сервер
rectangle  {
  
  (Авторизація) <. (реєстрація) : розширення
  (Пошук вільного місця) <. (Авторизація) : розширення

користувач --> (Пошук вільного місця)
користувач --> (Вибір абонементу)
    
  (Пошук вільного місця) --> сервер
(вибрати зону, вибрати місце, ввести номера машини, вибрати кількість годин, вивід кількості вільних місць)<.(Пошук вільного місця)  :включає
(Вибір абонементу) --> сервер 
(вибрати абонемент, вивід абонементів) <.(Вибір абонементу)  :включає
}
@enduml