@startuml
left to right direction
actor "Сервер" as fk
package "Передача данних до клієнта"{
  (надсилання інформації про вільні місця) <-- fk
  (надсилання інформації про абонементи) <-- fk
  (надсилання підтвердження про підключення) <-- fk
}
package "Сервер"{
  fk --> (Обробка запиту)
  (Обробка запиту) --> (Додавання нових користувачів)
  (Обробка запиту) --> (Пошук вільного місця)
(Обробка запиту) --> (Вибір вільного міця)
(Обробка запиту) --> (Пошук абонементів)
(Обробка запиту) --> (Вибір абонемента)	
 }
package "База даних"{
  (Обробка запиту) --> (Видалити дані)
  (Обробка запиту) --> (Додати дані)
  (Обробка запиту) --> (Редагувати дані)
}
@enduml